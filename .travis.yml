language: python
dist: xenial
sudo: true

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip3 install coveralls
  - pip3 install .

script:
  - coverage run -m pytest && coverage combine

after_success:
  - coveralls
