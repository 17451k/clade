language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
install:
  - pip install coveralls
script:
  - pip install .
  - coverage run -m pytest && coverage combine
after_success:
  - coveralls
